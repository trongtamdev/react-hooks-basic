import React from 'react';
import PropTypes from 'prop-types';

Clock.propTypes = {
    
};

function Clock(props) {
    return (
        <div>
            
        </div>
    );
}

export default Clock;