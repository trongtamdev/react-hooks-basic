import React from 'react';
import PropTypes from 'prop-types';

PostFiltersForm.propTypes = {
    onSubmit: PropTypes.func,
};

PostFiltersForm.defaul

function PostFiltersForm(props) {
    return (
        <div>
            
        </div>
    );
}

export default PostFiltersForm;